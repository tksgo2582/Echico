//33 퇴사
#include <bits/stdc++.h>
using namespace std;

int N;
vector<int> T, P;

int main(){
    cin >> N;
    int t,p;
    for(int i = 0 ; i < N; i++){
        cin >> t >> p;
        T.push_back(t);
        P.push_back(p);
    }
}